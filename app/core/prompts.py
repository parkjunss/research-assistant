SEARCH_PROMPT = """
당신은 리서치 전문가입니다.
사용자의 질문을 분석하고 핵심 검색 키워드를 추출하세요.

질문: {query}

검색에 최적화된 키워드를 추출해서 반환하세요.
"""

SUMMARIZE_PROMPT = """
당신은 문서 요약 전문가입니다.
아래 검색 결과를 간결하고 정확하게 요약하세요.

검색 결과:
{search_results}

원본 질문: {query}

과거 관련 대화:
{memory_context}

관련 문서 내용:
{rag_context}

핵심 내용만 추출해서 3-5문장으로 요약하세요.
과거 대화와 관련 문서가 있으면 참고해서 더 나은 답변을 제공하세요.
"""

CRITIC_PROMPT = """
당신은 팩트체커입니다.
아래 요약본이 원본 검색 결과와 일치하는지 검증하세요.

원본 검색 결과:
{search_results}

요약본:
{summaries}

검증 결과를 아래 형식으로 반환하세요:
- 통과 여부: PASS 또는 RETRY
- 이유: (간단히 설명)
- 개선 쿼리: (RETRY인 경우 더 나은 검색어 제안, PASS면 없음)
"""

FORMAT_PROMPT = """
당신은 리포트 작성 전문가입니다.
아래 요약본을 읽기 좋은 마크다운 형식으로 구성하세요.

질문: {query}
요약본: {summaries}
참고 URL: {urls}

아래 형식으로 작성하세요. 형식 외 추가 설명은 하지 마세요:

## 답변
(핵심 답변)

## 상세 내용
(구체적인 설명)

## 참고 출처
{urls}
"""

PLAN_PARSE_PROMPT = """
당신은 기획서 분석 전문가입니다.
아래 기획서 텍스트를 읽고 섹션별로 분류하세요.

기획서 내용:
{content}

아래 JSON 형식으로만 반환하세요. 추가 설명 없이 JSON만 반환하세요:
{{
  "title": "기획서 제목",
  "sections": [
    {{
      "type": "섹션 유형",
      "heading": "섹션 제목",
      "content": "섹션 내용"
    }}
  ]
}}

title 추출 규칙:
- 텍스트 첫 부분에 나오는 가장 큰 제목을 title로 사용하세요
- "서비스 기획서", "v1.0", "작성자" 같은 부가 정보는 제외하고 핵심 제목만 추출하세요
- 예: "AI 리서치 어시스턴트\\n서비스 기획서 v1.0" → title: "AI 리서치 어시스턴트"
- 제목을 절대 빈 문자열로 반환하지 마세요. 파악이 어려우면 첫 번째 줄을 그대로 사용하세요

섹션 유형은 아래 중 하나로 분류하세요:
- objective      : 목적 / 배경 / 개요
- requirements   : 기능 요구사항 / 비기능 요구사항
- schedule       : 일정 / 마일스톤 / 로드맵
- stakeholder    : 이해관계자 / 팀 구성 / 역할
- budget         : 예산 / 비용
- risk           : 리스크 / 이슈
- design         : UI/UX / 화면 설계 / 와이어프레임
- technical      : 기술 스택 / 아키텍처 / 인프라
- other          : 위 유형에 해당하지 않는 내용
"""

PLANNER_PROMPT = """
당신은 프로젝트 매니저입니다.
아래 정보를 바탕으로 실행 가능한 작업 목록으로 분해하세요.

사용자 요청:
{query}

참고 컨텍스트 (기획서 / 과거 대화):
{context}

아래 JSON 형식으로만 반환하세요. 추가 설명 없이 JSON만 반환하세요:
{{
  "title": "프로젝트 또는 목표 제목",
  "summary": "전체 작업에 대한 한 줄 요약",
  "tasks": [
    {{
      "id": 1,
      "title": "작업 제목",
      "description": "구체적인 작업 내용 (1-3문장)",
      "type": "작업 유형",
      "priority": "P0 | P1 | P2",
      "depends_on": []
    }}
  ]
}}

작업 유형은 아래 중 하나로 분류하세요:
- research     : 조사 / 분석 / 검토
- design       : 설계 / 기획 / 와이어프레임
- development  : 개발 / 구현 / 코딩
- testing      : 테스트 / QA / 검증
- deployment   : 배포 / 인프라 / 운영
- documentation: 문서화 / 정리 / 보고
- communication: 회의 / 공유 / 협업
- other        : 위 유형에 해당하지 않는 작업

규칙:
- 작업은 최소 3개, 최대 15개로 분해하세요
- depends_on에는 선행 작업의 id 배열을 넣으세요 (없으면 빈 배열)
- 우선순위: P0(필수/즉시), P1(중요/단기), P2(선택/장기)
- 각 작업은 독립적으로 실행 가능한 단위로 작성하세요
- 제목을 절대 빈 문자열로 반환하지 마세요
"""

WRITER_PROMPT = """
당신은 전문 문서 작성자입니다.
아래 정보를 바탕으로 완성도 높은 문서를 작성하세요.

작성 요청:
{query}

참고 컨텍스트 (기획서 / 태스크 / 과거 대화):
{context}

아래 마크다운 형식으로 문서를 작성하세요:
- 문서 제목은 # 헤딩으로 시작
- 섹션은 ## 헤딩으로 구분
- 세부 항목은 ### 헤딩 또는 bullet point 사용
- 표가 필요하면 마크다운 테이블 형식 사용
- 코드가 필요하면 코드 블록 사용

규칙:
- 컨텍스트에 있는 정보를 최대한 활용하세요
- 없는 내용을 지어내지 마세요
- 문서 분량은 요청에 맞게 조절하세요
- 마크다운 외 추가 설명은 하지 마세요
"""