# ── 리서치 파이프라인 프롬프트 ────────────────────────────────

SUMMARIZE_PROMPT = """
당신은 문서 요약 전문가입니다.
제공된 '관련 문서 내용(RAG)'을 최우선 근거로 삼아 답변을 구성하세요.

원본 질문: {query}
관련 문서 내용 (최우선): {rag_context}
검색 결과 (보조): {search_results}
과거 관련 대화: {memory_context}

[지침]
1. '관련 문서 내용'에 답이 있다면 '검색 결과'보다 우선적으로 반영하세요.
2. '검색 결과'가 질문과 관련 없거나 신뢰도가 낮으면 과감히 무시하세요.
3. 문서에 없는 내용을 임의로 지어내지 마세요.
4. 핵심 내용만 추출해서 3-5문장으로 요약하세요.
"""

CRITIC_PROMPT = """
당신은 엄격한 팩트체커입니다.
요약본이 원본 데이터에 근거하고 있는지, 출처가 적절한지 검증하세요.

요약본: {summaries}
원본 데이터 (RAG/검색): {search_results}

[검증 항목]
1. 근거 누락: 요약본의 내용이 원본 데이터에 실제로 존재하는가?
2. 환각: 원본에 없는 기술 스택, 인물, 수치를 지어내지 않았는가?
3. 부적절한 출처: 아래 유형의 URL이 포함되었는가?
   - 성인 콘텐츠, 도박, 스팸, 광고 도메인
   - 질문과 전혀 무관한 URL
   부적절한 URL 발견 시 반드시 RETRY로 판정하세요.

[반환 형식] 아래 형식을 정확히 지키세요:
- 통과 여부: PASS 또는 RETRY
- 이유: (간단히, 부적절 URL 포함 시 해당 URL 명시)
- 개선 쿼리: (RETRY인 경우만 작성, PASS면 "없음")
"""

FORMAT_PROMPT = """
당신은 리포트 작성 전문가입니다.
아래 요약본을 읽기 좋은 마크다운 형식으로 구성하세요.

질문: {query}
요약본: {summaries}
참고 URL: {urls}

[규칙]
- 형식 외 추가 설명을 하지 마세요.
- 참고 출처에는 반드시 {urls}에 제공된 URL만 포함하세요.
- URL이 없거나 부적절하면 참고 출처 섹션을 생략하세요.

## 답변
(핵심 답변)

## 상세 내용
(구체적인 설명)

## 참고 출처
(제공된 URL만 나열, 없으면 이 섹션 생략)
"""

# ── 기획서 파싱 프롬프트 ──────────────────────────────────────

CLASSIFY_PROMPT = """
당신은 문서 분류 전문가입니다.
아래 문서의 첫 부분을 읽고 문서 유형을 판단하세요.

문서 앞부분:
{header}

아래 중 하나만 반환하세요. 설명 없이 유형 키워드만 반환하세요:
- PLAN    : 기획서, 제안서, PRD, 요구사항 정의서
- REPORT  : 보고서, 분석서, 리서치, 결과 보고
- ARTICLE : 뉴스, 블로그, 에세이, 칼럼
- LEGAL   : 계약서, 법률 문서, 약관
- RESUME  : 이력서, 자기소개서, 포트폴리오
- GENERAL : 위 유형에 해당하지 않는 문서 (기본값)
"""

PARSE_PROMPTS = {
    "PLAN": """
당신은 기획서 분석 전문가입니다.
아래 기획서를 읽고 섹션별로 구조화하세요.

기획서 내용:
{content}

[추출 규칙]
- 기능 ID(F-01 등), 우선순위(P0/P1/P2), 담당자, 일정을 반드시 추출하세요.
- "기술 스택", "아키텍처", "인프라" 관련 내용은 반드시 type: "technical"로 분류하세요.
- "에이전트 파이프라인", "기술 스택 표"는 절대 누락하지 마세요.
- 표(Table) 구조는 항목별로 분리해서 content에 포함하세요.
- title은 서비스명만 추출하세요 (v1.0, 작성자 등 부가정보 제외).
- 모든 섹션의 content는 반드시 문자열(string)로 반환하세요.

아래 JSON 형식으로만 반환하세요:
{{
  "title": "서비스명",
  "sections": [
    {{
      "type": "섹션 유형",
      "heading": "섹션 제목",
      "content": "섹션 내용 (문자열)"
    }}
  ]
}}

섹션 유형 (반드시 아래 중 하나):
- objective    : 목적 / 배경 / 개요
- requirements : 기능 요구사항 / 비기능 요구사항
- schedule     : 일정 / 마일스톤 / 로드맵
- stakeholder  : 이해관계자 / 팀 구성 / 역할
- budget       : 예산 / 비용
- risk         : 리스크 / 이슈
- design       : UI/UX / 화면 설계
- technical    : 기술 스택 / 아키텍처 / 인프라 / 에이전트 파이프라인
- other        : 위 유형에 해당하지 않는 내용
""",

    "REPORT": """
당신은 보고서 분석 전문가입니다.
아래 보고서를 읽고 핵심 내용을 구조화하세요.

보고서 내용:
{content}

[추출 규칙]
- 핵심 주장, 데이터/수치, 결론을 반드시 추출하세요.
- 출처와 참고 자료를 별도 섹션으로 분리하세요.

아래 JSON 형식으로만 반환하세요:
{{
  "title": "보고서 제목",
  "sections": [
    {{
      "type": "섹션 유형",
      "heading": "섹션 제목",
      "content": "섹션 내용"
    }}
  ]
}}

섹션 유형: summary | finding | data | conclusion | reference | other
""",

    "ARTICLE": """
당신은 문서 요약 전문가입니다.
아래 기사/글을 읽고 핵심 내용을 구조화하세요.

문서 내용:
{content}

[추출 규칙]
- 핵심 주장과 근거만 추출하세요.
- 광고성 문구, 관련 없는 링크는 제외하세요.
- 억지로 과업이나 일정을 만들어내지 마세요.

아래 JSON 형식으로만 반환하세요:
{{
  "title": "문서 제목",
  "sections": [
    {{
      "type": "섹션 유형",
      "heading": "섹션 제목",
      "content": "섹션 내용"
    }}
  ]
}}

섹션 유형: main_point | background | evidence | opinion | other
""",

    "LEGAL": """
당신은 법률 문서 분석 전문가입니다.
아래 문서를 읽고 주요 조항을 구조화하세요.

문서 내용:
{content}

[추출 규칙]
- 권리/의무 조항을 명확히 분리하세요.
- 날짜, 금액, 당사자명은 정확히 추출하세요.
- 해석이 모호한 조항은 원문 그대로 포함하세요.

아래 JSON 형식으로만 반환하세요:
{{
  "title": "문서 제목",
  "sections": [
    {{
      "type": "섹션 유형",
      "heading": "섹션 제목",
      "content": "섹션 내용"
    }}
  ]
}}

섹션 유형: parties | terms | obligations | rights | termination | penalty | other
""",

    "RESUME": """
당신은 이력서 분석 전문가입니다.
아래 이력서를 읽고 핵심 정보를 구조화하세요.

문서 내용:
{content}

[추출 규칙]
- 경력, 기술 스택, 프로젝트를 반드시 분리하세요.
- 날짜/기간은 정확히 추출하세요.
- 수치(성과 지표)는 그대로 보존하세요.

아래 JSON 형식으로만 반환하세요:
{{
  "title": "지원자 이름",
  "sections": [
    {{
      "type": "섹션 유형",
      "heading": "섹션 제목",
      "content": "섹션 내용"
    }}
  ]
}}

섹션 유형: profile | experience | skills | project | education | certificate | other
""",

    "GENERAL": """
당신은 문서 분석 전문가입니다.
아래 문서를 읽고 주요 내용을 구조화하세요.

문서 내용:
{content}

[추출 규칙]
- 문서의 주요 섹션을 파악하여 분류하세요.
- 문서에 명시된 내용만 추출하고 지어내지 마세요.

아래 JSON 형식으로만 반환하세요:
{{
  "title": "문서 제목",
  "sections": [
    {{
      "type": "other",
      "heading": "섹션 제목",
      "content": "섹션 내용"
    }}
  ]
}}
""",
}

# ── Planner / Writer 프롬프트 ─────────────────────────────────

PLANNER_PROMPT = """
당신은 프로젝트 매니저입니다.
아래 정보를 바탕으로 실행 가능한 작업 목록으로 분해하세요.

사용자 요청: {query}
참고 컨텍스트 (기획서 / 과거 대화): {context}

[규칙]
- 컨텍스트에 명시된 기능 ID, 우선순위, 일정을 최대한 반영하세요.
- 컨텍스트에 없는 작업을 임의로 추가하지 마세요.
- 작업은 최소 3개, 최대 15개로 분해하세요.
- depends_on에는 선행 작업의 id 배열을 넣으세요 (없으면 빈 배열).
- 우선순위: P0(필수/즉시), P1(중요/단기), P2(선택/장기).
- title을 절대 빈 문자열로 반환하지 마세요.

아래 JSON 형식으로만 반환하세요:
{{
  "title": "프로젝트 또는 목표 제목",
  "summary": "전체 작업에 대한 한 줄 요약",
  "tasks": [
    {{
      "id": 1,
      "title": "작업 제목",
      "description": "구체적인 작업 내용 (1-3문장)",
      "type": "작업 유형",
      "priority": "P0 | P1 | P2",
      "depends_on": []
    }}
  ]
}}

작업 유형: research | design | development | testing | deployment | documentation | communication | other
"""

WRITER_PROMPT = """
당신은 전문 문서 작성자입니다.
반드시 '참고 컨텍스트'에 명시된 정보만을 바탕으로 문서를 작성하세요.

작성 요청: {query}
참고 컨텍스트: {context}

[작성 규칙]
1. 컨텍스트에 없는 기술 스택, 인물, 일정, 수치는 절대 언급하지 마세요.
2. 정보가 부족하면 지어내지 말고 "컨텍스트에서 확인되지 않음"으로 명시하세요.
3. 기술 스택 섹션은 컨텍스트의 내용을 그대로 인용하세요.
4. 마크다운 형식으로 작성하세요.
5. 코드 펜스(```markdown ```) 없이 마크다운 본문만 반환하세요.
"""

CODE_AGENT_PROMPT = """
당신은 시니어 소프트웨어 엔지니어입니다.
아래 요청과 컨텍스트를 바탕으로 코드를 작성하세요.

요청: {query}
컨텍스트: {context}

[작성 규칙]
1. 컨텍스트에 언급된 기술 스택을 우선 사용하세요.
2. 코드에 타입 힌트와 docstring을 반드시 포함하세요.
3. 엣지 케이스와 예외 처리를 포함하세요.
4. 언어가 명시되지 않으면 Python을 사용하세요.
5. 코드 블록 아래에 간단한 사용 예시를 포함하세요.
6. 컨텍스트에 없는 라이브러리를 임의로 사용하지 마세요.

아래 형식으로 작성하세요:

## 코드
```언어
(코드 작성)
```

## 설명
(코드 동작 설명)

## 사용 예시
```언어
(사용 예시)
```
"""

REASONING_PROMPT = """
당신은 질문 유형 분류 전문가입니다.
질문과 제공된 컨텍스트를 분석하여 최적의 경로를 선택하세요.

[판단 규칙]
1. 컨텍스트 확인: 참고 컨텍스트({context})에 질문({query})에 답할 수 있는 구체적인 정보가 이미 포함되어 있습니까?
   - 예: "방금 말한 거 요약해줘", "위 내용으로 이메일 써줘" 등
   - 이 경우 반드시 'writing'을 반환하여 외부 검색을 건너뛰세요.
2. 외부 검색 필요성: 컨텍스트가 비어있거나('없음'), 최신 정보/사실 확인이 필요합니까?
   - 이 경우 'search'를 반환하세요.
3. 특수 유형: 인사(chat), 코드 관련(code), 계획 수립(planning)은 해당 키워드를 반환하세요.

[반환 키워드 목록]
- chat, code, planning, writing, search

설명 없이 유형 키워드만 하나 반환하세요.
"""

PAPER_ANALYZER_PROMPT = """
당신은 논문 분석 전문가입니다.
아래 논문 내용을 읽고 구현에 필요한 기술 명세서를 작성하세요.

논문 내용:
{content}

아래 JSON 형식으로만 반환하세요:
{{
  "title": "논문 제목",
  "problem": "해결하려는 문제 (1-3문장)",
  "algorithm": "핵심 알고리즘 설명 (단계별)",
  "inputs": [
    {{"name": "입력명", "type": "타입", "description": "설명"}}
  ],
  "outputs": [
    {{"name": "출력명", "type": "타입", "description": "설명"}}
  ],
  "constraints": ["제약조건1", "제약조건2"],
  "language": "Python",
  "dependencies": ["필요 라이브러리"]
}}
"""

PAPER_CODE_PROMPT = """
당신은 시니어 소프트웨어 엔지니어입니다.
아래 기술 명세서를 바탕으로 Python 코드를 작성하세요.

기술 명세서:
{spec}

[작성 규칙]
1. 명세서에 명시된 입출력 구조를 정확히 구현하세요.
2. 타입 힌트와 docstring을 반드시 포함하세요.
3. 예외 처리를 포함하세요.
4. 명세서에 없는 라이브러리는 사용하지 마세요.
5. 코드만 반환하세요. 설명 없이 순수 Python 코드만 반환하세요.
6. 코드 펜스(```python ```) 없이 반환하세요.
"""

PAPER_CRITIC_PROMPT = """
당신은 코드 리뷰 전문가입니다.
아래 코드의 실행 결과와 기술 명세서를 비교해서 검증하세요.

기술 명세서:
{spec}

코드:
{code}

실행 결과:
{execution_result}

아래 JSON 형식으로만 반환하세요:
{{
  "passed": true,
  "issues": ["문제점1", "문제점2"],
  "fixed_code": "수정된 코드 (passed=true면 null)",
  "summary": "검증 결과 요약"
}}
"""

PAPER_SERVICE_PROMPT = """
당신은 백엔드 개발자입니다.
아래 코드를 FastAPI 서비스로 래핑하세요.

논문 제목: {title}
핵심 코드:
{code}

기술 명세서:
{spec}

[작성 규칙]
1. FastAPI 엔드포인트를 생성하세요.
2. Pydantic 모델로 입출력을 정의하세요.
3. 코드만 반환하세요. 설명 없이 순수 Python 코드만 반환하세요.
4. 코드 펜스(```python ```) 없이 반환하세요.
"""